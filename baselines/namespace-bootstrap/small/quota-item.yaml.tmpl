- apiVersion: v1
  kind: ResourceQuota
  metadata:
    name: ${NS_NAME}-quota
    annotations:
      baseline.kratix.io/version: ${BASELINE_VERSION}
      baseline.kratix.io/size: ${BASELINE_SIZE}
    namespace: ${NS_NAME}
  spec:
    hard:
      requests.cpu: 500m
      limits.cpu: "1"
      requests.memory: 1Gi
      limits.memory: 2Gi
      pods: "20"
