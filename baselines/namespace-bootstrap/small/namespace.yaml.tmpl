apiVersion: v1
kind: Namespace
metadata:
  name: ${NS_NAME}
  labels:
    project: ${PROJECT}
    team: ${TEAM}
    environment: ${ENV}
    area: ${AREA}
    baseline.kratix.io/version: ${BASELINE_VERSION}
    baseline.kratix.io/size: ${BASELINE_SIZE}
  annotations:
    baseline.kratix.io/version: ${BASELINE_VERSION}
    baseline.kratix.io/size: ${BASELINE_SIZE}
