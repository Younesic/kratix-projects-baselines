- apiVersion: v1
  kind: ResourceQuota
  metadata:
    name: ${NS_NAME}-quota
    annotations:
      baseline.kratix.io/version: ${BASELINE_VERSION}
      baseline.kratix.io/size: ${BASELINE_SIZE}
    namespace: ${NS_NAME}
  spec:
    hard:
      requests.cpu: "2"
      limits.cpu: "4"
      requests.memory: 4Gi
      limits.memory: 8Gi
      pods: "80"
