- apiVersion: v1
  kind: ResourceQuota
  metadata:
    name: ${NS_NAME}-quota
    namespace: ${NS_NAME}
  spec:
    hard:
      requests.cpu: "2"
      limits.cpu: "4"
      requests.memory: 4Gi
      limits.memory: 8Gi
      pods: "80"
